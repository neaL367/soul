//! Browser application entry point, wiring chrome, core state machines, and rendering engines together.

fn main() {
    common::init_tracing();
    tracing::info!("Soul Browser starting up (Milestone 0 skeleton)...");
}
